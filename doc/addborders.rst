AddBorders
==========

.. function::   AddBorders(clip clip[, int left=0, int right=0, int top=0, int bottom=0])
   :module: std
   
   Adds borders to frames. The arguments are the number of pixels to add and must obey the subsampling restrictions.
   The newly added borders will be zeroed memory.